# Copy to .env and fill in values (never commit .env)

# Algolia — required for /api/wines/* search
ALGOLIA_APP_ID=
ALGOLIA_API_KEY=

# Supabase — scripts and API as wired in your deployment
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# Auth for protected routes
JWT_SECRET=
